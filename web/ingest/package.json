{
  "name": "ingest",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "ChirpStack MQTT uplink bridge - subscribes to device uplinks and writes events/health rows into Supabase.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -b",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.2",
    "dotenv": "^16.4.7",
    "mqtt": "^5.10.3"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "tsx": "^4.19.2",
    "typescript": "~6.0.2"
  }
}
