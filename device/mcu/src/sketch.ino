// Required by arduino-cli's sketch convention: a sketch's main file must be
// named "<sketch-folder-name>.ino" and physically exist, or compile fails
// before touching any .cpp file ("main file missing from sketch"). setup()
// and loop() are implemented as plain functions in main.cpp, not here - this
// file exists only to satisfy that naming requirement.
